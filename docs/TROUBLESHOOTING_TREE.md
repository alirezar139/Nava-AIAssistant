# درختواره عیب‌یابی

## هدف

درختواره عیب‌یابی مسیر گفتگوی مرحله‌ای پشتیبانی را مشخص می‌کند. دستیار قبل از
جست‌وجوی FAQ یا ساخت تیکت، مسیر مشکل را به شکل ساختاریافته از کاربر دریافت
می‌کند.

## محل پیاده‌سازی

| بخش                     | مسیر                                                                          |
| ----------------------- | ----------------------------------------------------------------------------- |
| داده درخت               | `src/assets/troubleshooting-tree.json`                                        |
| مدل TypeScript          | `src/app/core/models/troubleshooting-tree.models.ts`                          |
| سرویس بارگذاری و پیمایش | `src/app/core/services/troubleshooting-tree.service.ts`                       |
| مصرف در جریان کاربر     | `src/app/features/assistant/pages/assistant-page/assistant-page.component.ts` |

## ساختار داده

```ts
interface TroubleshootingTree {
  startNodeId: string;
  introNodeIds: string[];
  nodes: TroubleshootingTreeNode[];
  edges: TroubleshootingTreeEdge[];
}
```

| فیلد           | کاربرد                                         |
| -------------- | ---------------------------------------------- |
| `startNodeId`  | اولین گره تصمیم که به کاربر نمایش داده می‌شود. |
| `introNodeIds` | گره‌های توضیحی حفظ‌شده از فایل مرجع.           |
| `nodes`        | گره‌های قابل نمایش یا واسط.                    |
| `edges`        | رابطه والد/فرزند و label اختیاری دکمه‌ها.      |

اندازه نسخه فعلی:

- ۳۰۶ گره
- ۳۴۳ ارتباط

فایل JSON فعلی با VSDX مرجع بررسی شده و در آن بررسی، گره یا ارتباط جاافتاده
مشاهده نشده است.

## جریان کاربر

1. دستیار گزینه‌های اولیه حوزه مشکل را نمایش می‌دهد.
2. کاربر گزینه‌ها را در درخت تصمیم انتخاب می‌کند.
3. هر label انتخاب‌شده به `treeTrail` اضافه می‌شود.
4. قبل از actionهای مرحله‌ای، snapshot گفتگو ذخیره می‌شود.
5. کاربر می‌تواند به snapshot قبلی برگردد.
6. کاربر می‌تواند گفتگو را از ابتدا شروع کند.
7. سامانه FAQها را با مسیر انتخاب‌شده جست‌وجو می‌کند.
8. اگر پاسخ قابل اتکا پیدا شود، همان پاسخ نمایش داده می‌شود.
9. اگر پاسخ قابل اتکا پیدا نشود، payload تشخیصی از مسیر انتخاب‌شده ساخته می‌شود.
10. بک‌اند پرونده داخلی و در صورت امکان تیکت سهند/Jira می‌سازد.
11. کاربر رسید تیکت و وضعیت روند پیگیری را می‌بیند.

## نمودار جریان

```text
نمایش گزینه‌های گره شروع
        |
        v
انتخاب گزینه توسط کاربر
        |
        v
ذخیره treeTrail و مرحله پشتیبانی
        |
        v
جست‌وجوی FAQ با مسیر انتخاب‌شده
        |
        +-- پاسخ قابل اتکا --> نمایش پاسخ تاییدشده
        |
        `-- بدون پاسخ قابل اتکا
                |
                v
        ساخت payload تشخیصی
                |
                v
        ساخت پرونده داخلی
                |
                v
        ارسال اختیاری تیکت سهند/Jira
                |
                v
        نمایش رسید و اطلاعات پیگیری
```

## تشخیص گره قابل نمایش

`TroubleshootingTreeService` می‌تواند گره‌های واسط کوتاه و بدون label معنادار را
رد کند تا کاربر مرحله‌های غیرضروری نبیند. مالک این رفتار متد زیر است:

```text
resolveDisplayNode
```

## نگاشت payload تیکت

وقتی جریان به ارجاع می‌رسد، دستیار از مسیر انتخاب‌شده payload تشخیصی می‌سازد:

| فیلد تشخیصی    | منبع                                 |
| -------------- | ------------------------------------ |
| `title`        | آخرین label معنادار.                 |
| `problem`      | مسیر انتخاب‌شده یا label حل‌نشده.    |
| `systemName`   | اولین حوزه معنادار.                  |
| `processName`  | labelهای میانی مسیر.                 |
| `scenario`     | کل مسیر انتخاب‌شده.                  |
| `serialNumber` | مقدار fallback در صورت نبود اطلاعات. |
| `errorText`    | label خطا یا متن fallback.           |
| `evidence`     | متن تولیدشده از جریان دستیار.        |

## نکته Citrix

Citrix به عنوان محیط کاری در نظر گرفته می‌شود، نه سناریوی جریان داده. مسیرهای
Citrix ممکن است به یکی از این محیط‌ها نگاشت شوند:

- `star-da1`
- `star-da2`
- `star-da3`
- `star-da4`

## قواعد نگهداری

- شناسه گره‌ها تا حد امکان پایدار بماند.
- هیچ گرهی بدون بررسی edgeهای ورودی و خروجی حذف نشود.
- زیر یک والد، label تکراری ساخته نشود.
- چرخه ایجاد نشود مگر سرویس پیمایش صریحا پشتیبانی کند.
- هر گزینه قابل نمایش باید مقصد قابل دسترس داشته باشد.
- بعد از جایگزینی فایل مرجع، مقایسه VSDX دوباره انجام شود.

## کارهای آینده

- انتقال JSON ثابت به مدل قابل مدیریت API.
- افزودن ویرایشگر گرافیکی مدیریتی برای گره‌ها و ارتباط‌ها.
- اعتبارسنجی چرخه، گره orphan، label تکراری و بن‌بست.
- افزودن تست خودکار برای مسیرهای مهم پشتیبانی.
